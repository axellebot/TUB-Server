{% extends '::tub_base.html.twig' %}
{% block page_content %}
    <div class=row">
        <div class="col s12 l6 offset-l3">
            {% include 'TubBundle:Schedule:list_card.html.twig' %}
        </div>
    </div>

    {% if is_granted('ROLE_ADMIN') %}
        <!-- Colored FAB button -->
        <div class="fixed-action-btn">
            <a class="btn-floating btn-large red" href="{{ path('schedule_create') }}">
                <i class="large material-icons">add</i>
            </a>
        </div>
    {% endif %}
{% endblock %}